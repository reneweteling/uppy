---
alwaysApply: true
---

Always use pnpm

To run a command dont forget to source the .envrc file, or run `direnv allow` like so

```bash
cd /Users/rene/projects/uppy && source .envrc && pnpm tauri dev
```
